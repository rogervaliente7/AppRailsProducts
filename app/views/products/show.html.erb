<%= image_tag @product.photo, width: 150 if @product.photo.attached? %>
<h1><%= @product.title %></h1>
<p><%= @product.description %></p>
<span> <%= @product.price %>$ </span>
<%= link_to 'Editar producto', edit_product_path(@product) %>
<%= link_to 'Eliminar producto', product_path(@product), method: :delete, data: {confirm: '¿Estas seguro?'} %>
<%#= link_to 'Eliminar producto', product_path(@product), data:{turbo_method: :delete} %>